[phases.setup]
aptPkgs = ["nodejs", "npm"]

[phases.install]
cmd = "cd backend && npm install"

[phases.build]
cmd = "cd backend && npm run build"

[start]
cmd = "cd backend && npm start"

[variables]
NODE_ENV = "production"
